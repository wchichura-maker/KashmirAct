# Knowledge graph

Seed only. This is not a runtime system.

`graph.json` records documents, decisions, P0 modules, and relations so later phases can update a single project graph.

Do not treat this graph as gameplay authority.

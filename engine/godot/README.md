# Godot adapter (provisional)

Future location for:

- scenes (`p0_main.tscn` and arena)
- GDScript adapters that bind Core/Game to Nodes
- AnimationTree / SkeletonModifier3D wiring
- debug draw that depends on the viewport

Do not put combat formulas here.

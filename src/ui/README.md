# UI

P0 requires a development overlay only: health, stamina, state, attack, target, telemetry count, pattern notification.

UI observes. UI is not an authority for gameplay state.

No HUD implementation in Bootstrap 0.
